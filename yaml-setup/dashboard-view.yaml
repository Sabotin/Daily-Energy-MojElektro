# Paste as a new view in the dashboard's raw configuration editor
# (Dashboard → ⋮ → Edit dashboard → ⋮ → Raw configuration editor, under "views:").
- title: Daily Energy
  path: energy
  type: panel
  cards:
    - type: custom:daily-energy-card
      entity: todo.daily_energy_log
      # Optional: HA user names that get the light version for slow tablets
      # lite_users:
      #   - tablet
